# Scripts

Utility scripts for testing and debugging.

## verify-dom-structure.js

Browser console script to verify DOM structure and find test selectors. Paste into browser console and run:

```javascript
DOMVerifier.runCompleteVerification();
```