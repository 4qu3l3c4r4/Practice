{
  "name": "pact-contract-template",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --runInBand",
    "test:publish": "npx pact-broker publish pacts --consumer-app-version=$(git rev-parse --short HEAD) --broker-base-url=$PACT_BROKER_URL"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^12.1.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}
